Return JSON only.

Task:
Search the web for candidate news articles from the curated outlets below.

This is a retrieval step only.
Do not answer the user question.
Do not write analysis.
Do not expose chain-of-thought.

Search strategy:
- Treat the planned search query as a starting point, not as fixed wording.
- Create several short search-engine-style variants before choosing candidates.
- Prefer headline-style wording likely to appear in news titles.
- Use aliases, synonyms, official names, legal terms, dates, numbers, and outlet-specific wording when useful.
- Keep domain restrictions such as `site:<domain>` when they are present.
- If the query is broad or conversational, rewrite it into sharper keyword queries.
- Use multiple internal searches if needed.

Variant generation:
Generate searches using these styles:
1. direct query
2. headline-style query
3. entity-alias query
4. official-source or legal/procedural query
5. broader fallback query

For legal/lawsuit questions, prefer words such as:
trial, testimony, lawsuit, court, judge, jury, damages, nonprofit, for-profit, cross-examination, witness stand, settlement, ruling, legal battle.

For numeric/casualty questions, prefer words such as:
death toll, killed, injured, wounded, casualties, tally, tracker, official figures, ministry, military, CENTCOM, HRANA.

Outlet diversity:
- Prefer candidates from multiple curated outlets.
- In a multi-outlet job, return at most one strong candidate per outlet when possible.
- Do not fill the result list with many articles from only one outlet.
- A plausible Reuters/CNN/France 24/Tasnim candidate is better than a fourth Al Jazeera or JPost candidate.
- Only return multiple candidates from the same outlet if fewer than 3 distinct outlets have plausible candidates.

Retrieval behavior:
- Return plausible candidates, not final selected sources.
- A candidate can be partial, older, or side-specific if it may help answer the query.
- Later pipeline stages will filter, select, and extract the final answer.
- Put visible evidence in `article_text` as one concise sentence.
- Reject only clearly unrelated articles.

Requirements:
- use only the curated outlets below
- prefer the last {days_back} days when possible
- allow older articles if more directly relevant
- return up to {outlet_limit} candidate articles total
- only return URLs whose hostname belongs to the listed outlet domain or valid subdomain
- keep `article_text` to one concise evidence hint
- return a JSON array and nothing else

For each object return exactly:
- title
- url
- outlet_name
- domain
- country
- medium_type
- orientation
- published_at
- snippet
- article_text
- search_query

Curated outlets:
{outlets_text}

Planned search queries:
{planned_queries_json}

User query:
{query}